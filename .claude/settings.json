{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; cd \"$root\" || exit 0; if [ -z \"$(git status --porcelain)\" ]; then exit 0; fi; git add -A && git commit -m \"auto: $(date -u +%Y-%m-%dT%H:%M:%SZ)\" >/dev/null 2>&1 && (git push 2>/dev/null || true); exit 0"
          }
        ]
      }
    ]
  }
}
